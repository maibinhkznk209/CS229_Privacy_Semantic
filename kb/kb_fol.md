Manual Translation Summary (FOL-ish)

## Statements derived from the paragraph

- company(google) ∧ actor(google) ∧ actor(user).
- collects(google, information).
- context(google_account).
- collects(google, personal_information) ∧ purpose(google, personal_information, create_or_use_account).
- collects_content(google, user_content).
- uses_technology(google, cookies).
- uses_technology(google, server_logs).
- collects_tech_data(google, technical_data).
- collects_tech_data(google, ip_address).
- varies_by(data_collection, service_usage) ∧ varies_by(data_collection, privacy_controls).
- stores_under_identifier(google, unique_identifier, not_signed_in, remember_preferences).
- uses_for(google, communicate_with_users).
- uses_for(google, improve_services).
- uses_for(google, maintain_services).
- uses_for(google, personalize_content_ads).
- uses_for(google, protect_from_fraud_abuse_security_risks).
- uses_for(google, provide_services).
- retains(google, data, retention_policy).
- allows_setting(google, auto_delete).
- allows_setting(google, delete).
- may_keep_longer_for(google, data, legal_needs).